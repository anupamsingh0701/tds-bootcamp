---
---

--- Before Day-1 ---
I already knew how to use basic Python, write simple scripts, and had some experience with VS Code. I had heard of Git before but never really used it properly. I knew what a terminal was but mostly avoided it.
---

## Day-1 Checklist

- [x] I can open a Linux terminal and recognize the shell prompt (`user@machine:~$`)
- [x] I can run `python --version`, `git --version`, and `uv --version` without errors
- [x] I can open VS Code connected to WSL/Linux using `code .`
- [x] I know what `~` means and can navigate to my home directory using `cd ~`
- [x] I can create folders and files using `mkdir` and `touch`, and list them with `ls`
- [x] I know the difference between `.xyz`, `./xyz`, and `../xyz`
- [x] I know that in Linux, everything is a file
- [x] I can navigate to the C and D drives within WSL
- [x] I understand the difference between `>` (overwrite) and `>>` (append)
- [x] I have a GitHub account and have created the `tds-bootcamp` repository

--- After Day-1 ---
I learned these things as well, apart from the checklist ...
I didn't know that WSL mounts Windows drives under /mnt/c and /mnt/d — that was a surprise. Also, I never knew that `touch` can update file timestamps, not just create files. The concept that "everything is a file" in Linux is something I had heard but now actually makes sense — even devices and processes are represented as files. I also got comfortable with `>>` for appending vs `>` for overwriting, which I used to confuse all the time.
---

--- Feedback (Suggestions for the TDS Team) ---
This is my feedback ...
The bootcamp is well structured and beginner friendly. It would be helpful to have a short cheat sheet PDF for common Linux commands to refer to offline. Also, a quick troubleshooting guide for WSL installation issues on different Windows versions would save a lot of time for students who get stuck during setup. Overall, Day-1 was smooth and motivating!
---

---

You can write your personal notes here; they will not be parsed and are for your own reference.
- Remember to practice `ls -la` to see hidden files
- The `.bashrc` file is at `~/.bashrc` — can customize the terminal prompt here
- Try `cat /etc/os-release` to check which Linux distro is running in WSL
---
